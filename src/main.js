import { createApp } from 'vue'
import App from './App.vue'
import store from "@/store";
import router from "@/router";
import elementPlsh from "element-plus";

createApp(App).use(store).use(router).use(elementPlsh).mount('#app')


























